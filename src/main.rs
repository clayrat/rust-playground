/*
pub mod bintree;
use bintree::BinaryTree;
use bintree::BinaryTree::*;
use bintree::TreeNode;

mod twothree;
*/

mod tree23;

fn main() {
    /*let jupiter_tree = NonEmpty(Box::new(TreeNode {
        element: "Jupiter",
        left: Empty,
        right: Empty,
    }));

    let mut tree = BinaryTree::Empty;
    tree.add("Venus");
    tree.add("Jupiter");
    tree.add("Mercury");
    tree.add("Mars");
    tree.add("Earth");
    tree.add("Saturn");
    tree.add("Neptune");
    tree.add("Uranus");

    println!("{:?}", tree.clone().inorder());

    let (tree,b) = tree.delete2("Neptune");
    println!("{:?} : {}", tree.clone().inorder(), b);

    let (tree,b) = tree.delete("Eris");
    println!("{:?} : {}", tree.clone().inorder(), b);

    println!("{}", tree.contains("Neptune"));

    let (tree,b) = tree.delete("Uranus");
    println!("{:?} : {}", tree.clone().inorder(), b);

    let (tree,b) = tree.delete("Ganymede");
    println!("{:?} : {}", tree.clone().inorder(), b);
*/
}
